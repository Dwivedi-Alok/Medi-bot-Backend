# Flask app and server
flask
flask-cors
python-dotenv

# LangChain + Integrations
langchain>=0.1.0
langchain-community>=0.0.22
langchain-core>=0.3.68
langchain-text-splitters>=0.3.8
langchain-groq
langchain-huggingface

# Embeddings + Models
huggingface-hub
sentence-transformers>=2.2.2
transformers
accelerate  # Sometimes required for HF inference

# Vector DB
faiss-cpu==1.11.0

# PDF Parsing
pypdf
pdfminer.six  # May be required by loaders

# Utility
tqdm
pydantic>=1.10.0,<2.0.0
requests
typing-extensions
setuptools>=65.5.0

# Gunicorn (for production WSGI server on Render)
gunicorn
